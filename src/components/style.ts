import styled from "styled-components";

export const Main = styled.main`
    display: flex;
    flex-direction: row;
    height: 100vh;
`;

export const Container = styled.div`
    padding: 40px;
    width: 100%;
`;
