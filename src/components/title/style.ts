import styled from "styled-components";

export const ContentTitle = styled.h1`
    font-size: 1.75rem;
    font-weight: 600;
    line-height: 32px;
    margin: 0;
`;
export const ContentDescription = styled.strong`
    font-size: 1rem;
    font-weight: 400;
    line-height: 18px;
`;

export const ContentContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 2em;
`;
